# REDLINE Unique Syntax Test

pub def get_boost(base: int) -> int:
    return base * 2

val ship_name: string = "Redline-One"
var velocity: int = 100
val boost: int = get_boost(velocity)

print(ship_name)
print("Velocity with boost:")
print(boost)
